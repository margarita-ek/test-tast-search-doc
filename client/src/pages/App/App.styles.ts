import styled from "styled-components"

export const StyledAppMain = styled.main`
    width: 100%;
    min-height: 100%;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    font-family: Arial;
`